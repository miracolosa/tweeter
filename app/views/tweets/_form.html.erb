<div>
  <%= simple_form_for tweet do |f| %>
    <%= f.input :body, placeholder: "What's happening?", label: false %>
    <div class="d-flex justify-content-end">
      <%= f.submit "Tweet", class: "btn-tweet-sm" %>
    </div>
  <% end %>
</div>
